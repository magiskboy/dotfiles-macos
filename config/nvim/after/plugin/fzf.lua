require('fzf-lua').setup({
    files = {
        file_icons = false,
    },
    winopts = {
        win_height = 0.85,
        win_width = 0.80,
        win_border = 'single',
        preview = {
            scrollbar = false,
        }
    },
})

